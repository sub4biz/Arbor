"""solution.py — the ONLY editable artifact in this pack (Arbor's edit surface).

Replace this with a working baseline implementation. It must be correct first;
Arbor will then optimize it to improve the score. Keep the entry-point signature
stable — `eval.py` / `task.py` call into it.
"""

from __future__ import annotations


def solve(problem):
    """TODO: return a solution for *problem*.

    The baseline should be the simplest correct implementation — it is the number
    Arbor has to beat, and it confirms the eval actually runs.
    """
    raise NotImplementedError("fill in the baseline solver")
